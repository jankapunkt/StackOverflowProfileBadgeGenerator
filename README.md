# StackOverflowProfileBadgeGenerator
A simple html page to configure your own stack overflow profile badge using the Stack Exchange 2.0 REST API. Requires jquery (not included). Current state: in development.
